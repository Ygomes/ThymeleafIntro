version: '3.8'
volumes:
  data:
services:
  db:
    image: mysql:8.0
    ports:
      - "3307:3306"
    volumes:
      - data:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=98513912
      - MYSQL_DATABASE=challenge